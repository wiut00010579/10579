<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Blog</title>
</head>
<body style="background-color: aliceblue;">
  <div class="container">
    <h1 class="mb-4">Blog blog</h1>
    <a href="/blog/new" class="btn btn-success">New Article</a>

    <% blog.forEach(blog => { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-title"><%= blog.title %></h4>
          <div class="card-subtitle text-muted mb-2">
            <%= blog.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2"><%= blog.description %></div>
          <div  style="color: rgb(22, 197, 60);">
            <%= blog.author %>
          </div>
          <a href="blog/<%= blog.slug %>" class="btn btn-primary">Read More</a>
          <a href="blog/edit/<%= blog.id %>" class="btn btn-info">Edit</a>
          <form action="/blog/<%= blog.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
    <div class="card-subtitle"><h1> ID : 00010579</h1></div>
  </div>
</body>
</html>